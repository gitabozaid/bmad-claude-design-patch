---
name: bmad-sync-artifacts
description: "Sync all BMAD artifacts after code changes are approved. Reads git diff, classifies changes (visual-only, content/field, new screen, restructure, new feature), and updates affected artifacts: page specs, API contracts, mock data, stories, epics, scenarios index, even PRD. Use when the user says 'sync artifacts', 'sync specs', 'update specs from code', 'sync changes', or after approving UI changes before shipping. Also trigger when the user says 'خلاص' during UI review — that's the signal to sync before ship. This skill is the glue that keeps design specs and code in sync."
---

Follow the instructions in ./workflow.md.
